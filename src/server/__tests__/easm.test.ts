import { describe, it, expect } from "vitest";
import { launchEASMScan } from "../easm";

describe("EASM Module", () => {
  it("should handle launching scans", async () => {
    expect(true).toBe(true);
  });
});
