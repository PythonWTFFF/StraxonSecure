import { describe, it, expect, vi } from "vitest";
import { getSOCAnalytics } from "../soc";

describe("SOC Analytics", () => {
  it("should be tested", async () => {
    expect(true).toBe(true);
  });
});
