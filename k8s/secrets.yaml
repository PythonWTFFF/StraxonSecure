apiVersion: v1
kind: Secret
metadata:
  name: straxon-secrets
type: Opaque
stringData:
  # IMPORTANT: Replace these with your actual production keys before applying
  VITE_SUPABASE_URL: "https://your-project.supabase.co"
  VITE_SUPABASE_ANON_KEY: "your-anon-key"
  # Any ML Engine secrets can go here as well
