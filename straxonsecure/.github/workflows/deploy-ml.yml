name: Deploy ML Engine (Render)

on:
  push:
    branches:
      - main
    paths:
      - '../straxon-ml-engine/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hook
        run: |
          curl -X POST -d '' ${{ secrets.RENDER_ML_DEPLOY_HOOK_URL }}
